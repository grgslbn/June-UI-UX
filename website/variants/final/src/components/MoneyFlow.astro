---
// Who pays whom — redesigned full-width diagram: Jij → (vaste jaarprijs) → June → (vergelijkt, regelt) → Leverancier,
// and the energy bill running back underneath, straight from supplier to you, bypassing June.
// `row` kept for backward compatibility (the layout is always the full-width one now).
interface Props { c: any; row?: boolean; }
const { c } = Astro.props;
const f = c.flow;
const fr = c.L === 'fr';
const role = fr
  ? { you: 'Choisissez votre abonnement et, si vous le souhaitez, validez chaque changement.', june: 'Ne vend pas d’énergie. Choisit votre contrat sur la base de votre consommation.', sup: 'Livre votre électricité et votre gaz, et vous envoie la facture, comme aujourd’hui.' }
  : { you: 'Kiest een abonnement en, als je wil, keurt elke overstap zelf goed.', june: 'Verkoopt geen energie. Kiest je contract op basis van jouw verbruik.', sup: 'Levert je stroom en gas en stuurt je de factuur, zoals nu.' };
---
<div class="mflow-c"><figure class="mflow" role="img" aria-label={`${f.you} → ${f.june}: ${f.pay}. ${f.june} → ${f.sup}: ${f.work}. ${f.bill}.`}>
  <div class="mfl-node"><span class="mfl-name">{f.you}</span><span class="mfl-role">{role.you}</span></div>
  <div class="mfl-edge mfl-edge--money" aria-hidden="true"><span class="mfl-label">{f.pay}</span><span class="mfl-arrow"></span></div>
  <div class="mfl-node mfl-node--june"><span class="mfl-name">{f.june}</span><span class="mfl-role">{role.june}</span></div>
  <div class="mfl-edge" aria-hidden="true"><span class="mfl-label">{f.work}</span><span class="mfl-arrow"></span></div>
  <div class="mfl-node"><span class="mfl-name">{f.sup}</span><span class="mfl-role">{role.sup}</span></div>
  <div class="mfl-bill" aria-hidden="true"><span class="mfl-bill-line"></span><span class="mfl-label">{f.bill}</span></div>
</figure></div>
