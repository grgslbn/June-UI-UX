{
  "name": "june-website",
  "private": true,
  "type": "module",
  "workspaces": ["template", "variants/*"],
  "scripts": {
    "build:all": "npm run build --workspaces --if-present",
    "check": "node tools/check-site.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "lighthouse": "^13.5.0",
    "playwright": "1.56.1"
  }
}
